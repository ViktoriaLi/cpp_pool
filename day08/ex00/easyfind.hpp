#include <iostream>

template <typename T>
void easyfind(T , int)
{
  int i = 0;
  while (i < len)
  {
    (*f)(array[i++]);
  }
}

template <typename Num>
void intTest(Num elem)
{
  std::cout << elem << std::endl;
}
